#ifndef MOVE_H
#define MOVE_H

enum class Move
{
    U, U2, U_, u, u_,
    F, F2, F_, f, f_,
    R, R2, R_, r, r_,
    B, B2, B_, b, b_,
    L, L2, L_, l, l_,
    D, D2, D_, d, d_,
    M, M2, M_,
    S, S2, S_,
    E, E2, E_,
    x, x2, x_,
    y, y2, y_,
    z, z2, z_
};

#endif